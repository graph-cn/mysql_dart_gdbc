name: mysql_dart_gdbc
description: A starting point for Dart libraries or applications.
version: 1.0.0
# repository: https://github.com/my_org/my_repo

environment:
  sdk: ^3.5.4

# Add regular dependencies here.
dependencies:
  # mysql_client_plus: ^0.0.30
  mysql_client:
    path: D:/work/CorvusYe/mysql.dart
  # dart_gdbc: ^0.0.4+1
  dart_gdbc: 
    path: ../../
  # path: ^1.8.0

dev_dependencies:
  lints: ^4.0.0
  test: ^1.24.0
